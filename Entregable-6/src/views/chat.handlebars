<div>
  {{#if messages}}
    {{#each messages}}
      <div>
        <p><strong>{{this.user}}</strong>: {{this.message}}</p>
      </div>
    {{/each}}
  {{else}}
    <p>No message</p>
  {{/if}}
</div>